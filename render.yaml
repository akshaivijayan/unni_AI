services:
  - type: web
    name: unni-ai-notes
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MISTRAL_API_KEY
        sync: false
      - key: MISTRAL_MODEL
        value: mistral-small-latest
    healthCheckPath: /health
